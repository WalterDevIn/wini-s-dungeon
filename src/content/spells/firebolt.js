export const firebolt = Object.freeze({
  id: "firebolt",
  projectile: Object.freeze({
    speed: 260,
    size: 10,
    damage: 3,
    lifetimeSeconds: 1.4,
    color: "#ff7a33",
    destroyOnWall: true,
    destroyOnHit: true,
  }),
});
